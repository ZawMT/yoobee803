## User Knowledge Modeling — Data Preprocessing

- No null values found
- Duplicate rows checked and removed if present
- Features scaled using StandardScaler
- Target encoded using LabelEncoder

**Note**: Some values are given as "very_low" and some "very low", and they are treated as the same.